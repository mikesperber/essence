(define (parse-result lhs dot inp)
  (vector lhs dot inp))
(define (result-lhs result)
  (vector-ref result 0))
(define (result-dot result)
  (vector-ref result 1))
(define (result-inp result)
  (vector-ref result 2))
(define parse-result? vector?)
