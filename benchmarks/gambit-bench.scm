(bench-setup! (cdr (vector->list ##argv)))
(define t1 (runtime))
(bench-run)
(define t2 (runtime))

(display "Gambit runtime: ")
(display (##flonum./ (##flonum.- t2 t1)
		     (##flonum.<-fixnum *bench-ntimes*)))
(display " seconds per run.")
(newline)
